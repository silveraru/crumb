services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: crumb
      POSTGRES_PASSWORD: crumb
      POSTGRES_DB: crumb
    ports: ["5432:5432"]
    volumes: [pgdata:/var/lib/postgresql/data]
volumes:
  pgdata:
